{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    // Node resolves no extension of its own: a script it runs directly must
    // spell out the `.ts` it imports, where a bundler would have guessed.
    "allowImportingTsExtensions": true,
    "types": ["node"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    // `scripts/` is run by Node, which erases types without compiling them: it
    // would not know how to render `enum`, `namespace` or constructor
    // properties. Without this option, such syntax would pass the typecheck and
    // break at launch.
    "erasableSyntaxOnly": true,
    "skipLibCheck": true,
    "noEmit": true,
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo"
  },
  "include": ["vite.config.ts", "scripts/**/*.ts"]
}
