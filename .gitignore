node_modules
dist
design
.env
.env.local
*.local
.DS_Store

# The issue body the API canary writes before `gh` reads it back.
canary-issue.md

# Working notes, kept local: they describe intentions and trade-offs, not what
# the repository ships. The README stays the public documentation.
DESIGN.md
PRODUCT.md

# Local tooling, reinstallable, unrelated to the product.
# The Impeccable skill installs twice over, once per agent convention;
# `.impeccable/` is its local state — hook cache and workstation configuration.
.claude/
.github/skills/
.github/hooks/
.impeccable/
